import React from 'react';
import MinifyHtmlComponent from '../../components/MinifyHtmlComponent';
import Layout from '../../components/Layout';

const MinifyHtml = () => (
  <Layout>
    <main>
      <MinifyHtmlComponent />
    </main>
  </Layout>
);

export default MinifyHtml;